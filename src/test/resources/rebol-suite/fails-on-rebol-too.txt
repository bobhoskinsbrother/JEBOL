# Assertions the Rebol this is measured against does not pass either.
#
# Two kinds, and neither is a gap. Some are wrong about the Rebol they came
# from, and JEBOL answers what that Rebol answers. Others are guarded on a
# feature 3.22.5 has not got, so they never run there at all. Putting either
# in known-gaps.txt would say there is work here and there is not, and
# deleting them would lose the finding.
#
# Nothing goes in here on reasoning. Each line was run through ./r3-head, the
# same 3.22.5 the port is measured against, and the answer written down below
# it. If a line here ever starts passing in JEBOL, JEBOL has diverged from
# Rebol and the line should come back out.
#
# ---------------------------------------------------------------------------
#
# make-test.r3 asks for bad-make-arg from `to char! #FF`. The same checkout's
# t-char.c has a `case REB_ISSUE` that scans the digits as hex, and ./r3-head
# answers #"ÿ". The comment beside the assertion points at issue 1130, which
# is what added that arm: the test was written before it and never updated
# after. JEBOL matches the C and the binary, so it stays failing.
#
#   >> all [error? e: try [to char! #FF]  e/id = 'bad-make-arg]
#   == none            ; r3-head 3.22.5, and JEBOL
#
make-test.r3 / make char! / to char! issue! #14

# series-test.r3 asks whether "🙂" equals the LENGTH? of a string whose index
# a SWAP has just invalidated -- a string against a number, false whatever the
# number turns out to be. Rebol's own comment on the line reads "Known
# issue!!!".
#
#   >> all ["b🙂" == swap s1: "🙂b" s2: next s1  "🙂" == length? s2]
#   == none            ; r3-head 3.22.5, and JEBOL
#
series-test.r3 / SWAP / swap invalidating index (known issue) #973

# ---------------------------------------------------------------------------
#
# Four whole files are guarded on natives 3.22.5 has not got, so a real Rebol
# runs none of their assertions. Running each through ./r3-head prints the
# count, and the count is nought:
#
#   $ r3-head units/handle-test.r3     -> Number of Assertions Performed: 0
#   $ r3-head units/chacha20-test.r3   -> Number of Assertions Performed: 0
#   $ r3-head units/poly1305-test.r3   -> Number of Assertions Performed: 0
#   $ r3-head units/aes-test.r3        -> Number of Assertions Performed: 0
#
# The guards are `if native? :chacha20`, `if native? :poly1305`,
# `if native? :aes`, and for the handles `if not error? try [h3: aes/key ...]`.
# JEBOL answers no-value for all four of those names, exactly as r3-head does,
# so the groups are skipped in both and the assertions inside them are not
# work anybody can do short of reviving a native Rebol deprecated.
#
# chacha20-test.r3 says so itself, above its guard:
#   ";- this tests are only for the now deprecated `chacha20` native function!"
#
aes-test.r3 / (no group) / (no test) #1
aes-test.r3 / (no group) / (no test) #2
aes-test.r3 / (no group) / (no test) #3
aes-test.r3 / (no group) / (no test) #4
chacha20-test.r3 / (no group) / (no test) #1
chacha20-test.r3 / (no group) / (no test) #10
chacha20-test.r3 / (no group) / (no test) #11
chacha20-test.r3 / (no group) / (no test) #12
chacha20-test.r3 / (no group) / (no test) #13
chacha20-test.r3 / (no group) / (no test) #14
chacha20-test.r3 / (no group) / (no test) #15
chacha20-test.r3 / (no group) / (no test) #16
chacha20-test.r3 / (no group) / (no test) #17
chacha20-test.r3 / (no group) / (no test) #18
chacha20-test.r3 / (no group) / (no test) #19
chacha20-test.r3 / (no group) / (no test) #2
chacha20-test.r3 / (no group) / (no test) #3
chacha20-test.r3 / (no group) / (no test) #4
chacha20-test.r3 / (no group) / (no test) #5
chacha20-test.r3 / (no group) / (no test) #6
chacha20-test.r3 / (no group) / (no test) #7
chacha20-test.r3 / (no group) / (no test) #8
chacha20-test.r3 / (no group) / (no test) #9
handle-test.r3 / context handles / (no test) #1
handle-test.r3 / context handles / (no test) #10
handle-test.r3 / context handles / (no test) #11
handle-test.r3 / context handles / (no test) #12
handle-test.r3 / context handles / (no test) #13
handle-test.r3 / context handles / (no test) #14
handle-test.r3 / context handles / (no test) #15
handle-test.r3 / context handles / (no test) #16
handle-test.r3 / context handles / (no test) #17
handle-test.r3 / context handles / (no test) #18
handle-test.r3 / context handles / (no test) #19
handle-test.r3 / context handles / (no test) #2
handle-test.r3 / context handles / (no test) #20
handle-test.r3 / context handles / (no test) #21
handle-test.r3 / context handles / (no test) #22
handle-test.r3 / context handles / (no test) #23
handle-test.r3 / context handles / (no test) #24
handle-test.r3 / context handles / (no test) #25
handle-test.r3 / context handles / (no test) #26
handle-test.r3 / context handles / (no test) #27
handle-test.r3 / context handles / (no test) #28
handle-test.r3 / context handles / (no test) #29
handle-test.r3 / context handles / (no test) #3
handle-test.r3 / context handles / (no test) #30
handle-test.r3 / context handles / (no test) #31
handle-test.r3 / context handles / (no test) #32
handle-test.r3 / context handles / (no test) #33
handle-test.r3 / context handles / (no test) #34
handle-test.r3 / context handles / (no test) #35
handle-test.r3 / context handles / (no test) #36
handle-test.r3 / context handles / (no test) #37
handle-test.r3 / context handles / (no test) #38
handle-test.r3 / context handles / (no test) #39
handle-test.r3 / context handles / (no test) #4
handle-test.r3 / context handles / (no test) #40
handle-test.r3 / context handles / (no test) #41
handle-test.r3 / context handles / (no test) #42
handle-test.r3 / context handles / (no test) #43
handle-test.r3 / context handles / (no test) #44
handle-test.r3 / context handles / (no test) #45
handle-test.r3 / context handles / (no test) #46
handle-test.r3 / context handles / (no test) #47
handle-test.r3 / context handles / (no test) #5
handle-test.r3 / context handles / (no test) #6
handle-test.r3 / context handles / (no test) #7
handle-test.r3 / context handles / (no test) #8
handle-test.r3 / context handles / (no test) #9
poly1305-test.r3 / (no group) / (no test) #1
poly1305-test.r3 / (no group) / (no test) #10
poly1305-test.r3 / (no group) / (no test) #2
poly1305-test.r3 / (no group) / (no test) #3
poly1305-test.r3 / (no group) / (no test) #4
poly1305-test.r3 / (no group) / (no test) #5
poly1305-test.r3 / (no group) / (no test) #6
poly1305-test.r3 / (no group) / (no test) #7
poly1305-test.r3 / (no group) / (no test) #8
poly1305-test.r3 / (no group) / (no test) #9

# compress-test.r3: the CRUSH group opens
#     either error? e: try [compress "test" 'crush][--assert 'feature-na = e/id][...]
# so the assertion in the first branch only runs on a build without CRUSH.
# A real 3.22.5 has it -- `compress "test" 'crush` answers
# #{04000000E894994307} -- so that branch is never taken there either, and
# the assertion is never executed by anything this is measured against.
compress-test.r3 / CRUSH compression / decompression / CRUSH compress/decompress #121

# compress-test.r3: the LZW group opens the same way as the CRUSH one above,
# and a real 3.22.5 has LZW too -- `compress "test" 'lzw` answers
# #{0774657374FF01} there -- so its first branch is never taken either.
compress-test.r3 / LZW compression / decompression / LZW compress/decompress #102

# make-test.r3 asks for an error from MAKE and TO of a map from a paren, and a
# real 3.22.5 builds the map instead. Found by audit on 5 September 2026, on
# the list as gaps until then.
#
#     >> mold try [make map! quote (1 2)]
#     == #[
#         1 2
#     ]
#     >> mold error? try [make map! quote (1 2)]
#     == #(false)
#     >> mold error? try [to map! quote (1 2)]
#     == #(false)
#
# So `error? try [...]` is false there, the assertion fails, and making it
# pass here would mean refusing a paren that Rebol accepts.
make-test.r3 / make/to map / make map! .. #498
make-test.r3 / make/to map / to map! .. #533

# module-test.r3 expects `module [type: :print][]` to raise wrong-type. It
# raises nothing at all on 3.22.5, so the first condition of the ALL is false
# and the whole assertion answers none. Written against issue 1444, which the
# checkout appears to have settled the other way.
#
#     >> mold all [error? err: try [module [type: :print][]] err/id = 'wrong-type]
#     == _
#     >> mold error? try [module [type: :print][]]
#     == #(false)
module-test.r3 / make module / make module type assertion #18

# ---------------------------------------------------------------------------
#
# The five below were cut out of the vendored suite files on 9 August 2026 and
# held in src/test/resources/rebol-suite-excluded/, with reasons taken against
# an R3 3.22.1 binary that no longer exists. That directory is gone: the
# assertions are back in the files where Rebol wrote them, and the ones a real
# 3.22.5 still fails are recorded here where the ratchet can see them.
#
# Most of the ledger's reasons had gone stale. `distance`, `as-color` and
# `factorial` were excluded as "not in this build of R3 at all" and all three
# are present and correct in 3.22.5; the files under units/ that three more
# were said to need have since been vendored. Twenty-four of the thirty-three
# pass here now. These five do not, and neither does Rebol.
#
#   >> mold all [error? e: try [bind 'id err] e/id = 'expect-arg]
#   == _
object-test.r3 / EXTEND object / bind to error #50

#   >> mold error? transcode/one/error "#{0}"
#   == #(false)
lexer-test.r3 / BINARY / binary! with comments inside #418

# Rebol's own test name says "known issues", and they are its own:
#   >> mold [[1] [2]]     == parse [1 2][collect some [collect keep integer!]]
#   == #(false)
#   >> mold [[1] a [2] a] == parse [1 2][collect some [collect keep integer! keep ('a)]]
#   == #(false)
#   >> mold [[1 a] [2 a]] == parse [1 2][collect some [collect [keep integer! keep ('a)]]]
#   == #(false)
parse-test.r3 / COLLECT/KEEP / block collect nested (known issues) #98
parse-test.r3 / COLLECT/KEEP / block collect nested (known issues) #99
parse-test.r3 / COLLECT/KEEP / block collect nested (known issues) #100
